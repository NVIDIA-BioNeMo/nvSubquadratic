# Development dependencies for nvSubquadratic (tools and notebooks only).
#
# torch / torchvision: defined in pyproject.toml [project.dependencies] with the
# same bounds as the runtime package. Do NOT list them here with a separate loose
# pin (e.g. torchvision>=0.15): pip would satisfy that from PyPI and can replace a
# CUDA-matched torch wheel (e.g. cu128 in Docker) with a different CUDA build and
# break Apex in slurm/enroot/Dockerfile.slurm.
#
# Install order for local dev: pip install -e ".[quack]"  (or plain .) then
#   pip install -r requirements-dev.txt

pytest>=6.0
pytest-cov>=2.0
ruff>=0.12.0
mdformat>=0.7.0
pre-commit>=4.0
detect-secrets>=1.5.0
jupyterlab>=4.0.0
clean-fid>=0.1.35
